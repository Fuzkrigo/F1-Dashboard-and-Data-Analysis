# =============================================================================
# requirements.txt — F1 Insights Engine
#
# [EN] Python runtime dependencies, pinned to exact versions for reproducible
# builds (local == production). Test-only deps are NOT listed here. When a
# version needs bumping, do it deliberately (Snyk/Dependabot) and re-test.
#
# [PT-BR] Dependências de runtime do Python, fixadas em versões exatas para
# builds reproduzíveis (local == produção). Dependências só de teste NÃO entram
# aqui. Para subir uma versão, faça de forma deliberada (Snyk/Dependabot) e
# re-teste.
#
# Author: Bruno Krieger
# =============================================================================

# --- API ---
fastapi==0.138.0
uvicorn==0.49.0

# --- Database ---
sqlalchemy==2.0.51
asyncpg==0.31.0
aiosqlite==0.22.1

# --- Telemetry (FastF1 live timing) ---
fastf1==3.8.3
pandas==2.3.3

# --- HTTP / Data Fetching ---
requests==2.34.2

# --- Configuration & Validation ---
python-dotenv==1.2.2
pydantic==2.13.4

# --- Pinned transitive dependencies (security) ---
pillow==12.2.0
urllib3==2.7.0

# =============================================================================
# STARTUP COMMANDS / COMANDOS DE INICIALIZAÇÃO
#
# --- Step 1: Install dependencies ---
#   pip install -r requirements.txt
#
# --- Step 2: Run ETL Pipeline (first time only) ---
#   python -m src.etl.run_pipeline --years 2023 2024 2025
#
# --- Step 3: Start API ---
#   python -m uvicorn src.api.main:app --reload --port 8000
#
# --- Step 4: Open frontend ---
#   cd src/web && python -m http.server 3000
#   Then open http://localhost:3000 in your browser
#
# --- Docker (all services) ---
#   docker-compose up --build
# =============================================================================
